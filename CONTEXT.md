# 実験データ管理

中規模でオンラインに繋がらない装置の前で、ユーザーが実験データを本システムの正本として残す文脈。予約・設備見える化・映像化・画像解析は外。ARIM RDE への送出は後続課題。ARIM RDE と同一の項目は ARIM RDE と同じ名称を使う。例外として、ARIM RDE における登録単位の呼称は「データ」であるが、本システムでは「実験データ」とする。

権限・制約などのドメインルールは `.kiro/specs/experimental-data-registration/requirements.md` に置く。本ファイルは用語集のみ。ARIM RDE の構造との対応表は `docs/research-arim-rde-export.md` に置く。

## Language

**ユーザー**:
中規模でオンラインに繋がらない装置の前で、記録を残す人。装置担当・グループ担当の役割を持てる。DICE ID・所属を任意で持つ。ARIM RDE へ送出するときは研究チームメンバ（MEMBER）に対応する。
_Avoid_: 実験者, 利用者, 外部利用者

**装置担当**:
ユーザーが持つ役割。固有情報の欄の定義と装置台帳の管理を担う。ARIM RDE でデータセットテンプレートを用意する装置担当に対応する。
_Avoid_: 管理者, 施設スタッフ

**グループ担当**:
ユーザーが持つ、グループごとの役割。グループのメンバー管理と課題の登録を担う。ARIM RDE へ送出するときは研究チーム管理者（OWNER）に対応する。ARIM RDE の管理者代理・データ登録代行者・データ閲覧者に当たる役割は本システムでは持たない。
_Avoid_: グループオーナー, グループ代表, グループ管理者, 研究チーム管理者

**システム管理者**:
本システムの運用者。ユーザーとは別で、記録は残さない。
_Avoid_: 管理者

**グループ**:
本システムのまとまり（大学の研究室でも、半導体関連企業でもよい）。英語は group。試料が属し、試料名が一意である範囲。課題を 0 個以上持つ。ARIM RDE の所属（organization）とは別概念で、研究チームとも 1:1 ではない。
_Avoid_: 研究室, 拠点, 施設, 課題, 所属, 研究チーム

**課題**:
ARIM の利用申請が採択されて課題番号（JPMXP12 から始まる 15 桁）が付いた単位。グループに属し、期間とデータセットを持つ。実験データの親ではなく、送出時に選ぶ先。
_Avoid_: プロジェクト, テーマ, グループ

**所属**:
ARIM RDE の organization。データ所有者や試料管理者の機関名。ユーザーの任意項目。
_Avoid_: グループ

**DICE ID**:
ARIM RDE の利用者識別子（UUID）。ユーザーが任意で持ち、後から本人が追記できる。実験データや試料には持たせず、送出時にデータ所有者・試料管理者・送出者のユーザーから引く。
_Avoid_: ARIM ID, RDE ID

**登録**:
本システムの正本に実験データが残り、後から直せる状態。ARIM RDE への送出は含まない。
_Avoid_: アップロード, データを上げる, ARIM登録

**送出**:
本システムの実験データを ARIM RDE のデータとして登録すること。送出パッケージを本システムが作り、ユーザーが RDE の画面で登録する。
_Avoid_: 登録, アップロード, 同期

**正本**:
記録の正式な置き場所。本システムが正本であり、ARIM RDE は後から出す先。
_Avoid_: 下書き, キャッシュ, SETI（[ARIM-SETI 半導体基盤プラットフォーム](https://nanonet.go.jp/seti/)の略称。正本の呼称としては使わない）

**実験データ**:
その場の装置 1 台・1 操作の記録であり、登録の単位。試料を 1 つ指す。ARIM RDE のデータ 1 件（dataId, データ名, データ所有者）に対応する。
_Avoid_: データ, 工程記録, データセット, 実験, 装置ラン, 工程

**データ所有者**:
実験データに属するユーザー参照。既定は登録したユーザーで、登録者と別にできる。権限は付かない。ARIM RDE のデータ所有者（dataOwner）に対応する。
_Avoid_: 登録者, 投入者

**説明**:
実験データに任意で付く自由記述。ARIM RDE の説明（description）に対応する。
_Avoid_: コメント, メモ

**データ名**:
ARIM RDE のデータの必須項目。本システムでは持たず、送出時に装置・試料名・実施日時から生成する。

**実験 ID**:
ARIM RDE のデータの任意項目。送出時に本システムの実験データ識別子を入れ、RDE 側から本システムへ戻る対応キーとする。

**データセット**:
ARIM RDE において複数のデータをまとめる単位。課題に属し、装置（データセットテンプレート）ごとに開設される。
_Avoid_: 実験データ, 工程記録

**送出記録**:
実験データとデータセットの対応、送出したユーザー、日時、RDE 側の結果（データ ID、正常終了・異常終了）の記録。
_Avoid_: 送出ログ, 同期状態

**試料**:
グループに属する実体で、実験データが指す対象。ARIM RDE の試料（SAMPLE）に対応する。参考 URL・タグ・試料の説明を任意で持つ。初回送出後に RDE の試料 UUID を記録する。
_Avoid_: サンプル

**試料名**:
ユーザーにとっての試料の識別子。ARIM RDE の試料名（ローカルID / sampleName）に対応する。
_Avoid_: 試料ID（ARIM RDE が振る ID との混同）

**試料管理者**:
試料に属するユーザー参照。既定は試料を作成したユーザーで、作成者と別にできる。権限は付かない。ARIM RDE の試料管理者に対応する。
_Avoid_: 試料作成者, 試料所有者

**訂正履歴**:
実験データまたは試料の過去の値の記録。最新の値が現在の内容として見える。

**装置台帳**:
本システムに登録された装置の一覧。

**装置**:
製造装置または検査装置。実験データが指す機械。ARIM RDE の装置（必須項目）に対応し、送出のために RDE 側の装置識別子・データセットテンプレート・データファイルの拡張子を持てる。
_Avoid_: 工程に紐づけた装置, 施設

**製造装置**:
試料の状態を変える装置。検査装置とは別種。
_Avoid_: 種別を曖昧にしたまま装置と呼ぶこと

**検査装置**:
試料を観察・測定する装置。製造装置とは別種。
_Avoid_: 種別を曖昧にしたまま装置と呼ぶこと

**化学式・組成式・分子式**:
試料に属する組成。ARIM RDE の sample.composition（化学式・組成式・分子式など）に対応する。
_Avoid_: マテリアルデータ

**固有情報**:
実験データに属する、その装置用のパラメータ。ARIM RDE の固有情報（invoiceCustom）に対応する。製造装置用と検査装置用がある。
_Avoid_: プロセスデータ, 検査パラメータ, マテリアルデータ

**データファイル**:
実験データに付くファイルのうち、ARIM RDE で構造化処理の対象となるもの。装置ごとに拡張子が決まる。添付とは区別する。
_Avoid_: 添付, 実験データファイル

**添付**:
実験データに任意で付くファイルのうち、データファイル以外のもの。ARIM RDE の添付ファイルに対応する。
_Avoid_: 実験データファイル, データファイル

**実施日時**:
その操作を行った時。登録した時（ARIM RDE の記入年月日）ではない。
_Avoid_: 記入年月日, 登録日時
