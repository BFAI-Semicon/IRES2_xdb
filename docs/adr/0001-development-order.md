# 手入力登録を先に、ARIM RDE 送出を次に置く

正本は本システムであり、実験データの登録単位と編集境界はローカルで完結させる。そのため開発は (1) Web 手入力による実験データ登録（グループ・試料・装置台帳・固有情報欄・装置担当・訂正履歴を含む）→ (2) ARIM RDE への送出、の順とする。OCR と API は入力経路の追加であり (1)(2) の後に置くが、相互の順序は未定とする。OCR/API を送出より先にする案は、正本への登録と送出の境界をぼかすため採用しない。
